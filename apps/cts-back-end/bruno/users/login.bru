meta {
  name: login
  type: http
  seq: 1
}

post {
  url: {{API_HOST}}/{{API_PREFIX}}/auth/login
  body: json
  auth: none
}

body:json {
  {
    "email": "kevin@test.com",
    "password": "xxxxxx"
  }
}
