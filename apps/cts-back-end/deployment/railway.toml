[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
startCommand = "node main.js"

[[services]]
internal_port = 3000
protocol = "http"
auto_deploy = true

[mount]
source = "."
destination = "/app"

[env]
PORT = "3000"
NODE_ENV = "production"
